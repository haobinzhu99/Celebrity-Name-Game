generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Game {
  id       Int     @id
  roomCode String?
  answers  String?
}

model Answer {
  id       Int     @id
  username String?
  celebrity String?
}
